# Week 40 Quiz — Phase 4 Gate: Integrated Knowledge

This quiz covers the full arc of Phase 4. Questions synthesize across multiple weeks rather than testing any single topic in isolation.

## Multiple Choice

**Q1.** You have a 7B model, 24 GB VRAM (A10G on RunPod), and 10K training examples for a PostgreSQL text-to-SQL task. You want to fine-tune with LoRA. Which configuration is the most defensible starting point?

A. rank=4, alpha=4, target_modules=[q_proj, v_proj], lr=2e-4  
B. rank=16, alpha=32, target_modules=[q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj], lr=2e-4  
C. rank=64, alpha=128, target_modules=[q_proj, v_proj], lr=5e-4  
D. rank=16, alpha=16, target_modules=[q_proj, v_proj], lr=1e-5

---

**Q2.** You run QLoRA fine-tuning on `Qwen/Qwen2.5-Coder-7B` with NF4 quantization and paged_adamw_8bit. After 500 steps your training loss is stuck at 1.2 — it was 1.8 at step 50, dropped to 1.3 by step 200, then plateaued. Which hypothesis is most likely?

A. The NF4 quantization is degrading gradient signal so severely that the model cannot learn further  
B. The learning rate is too high — the model has overshot the minimum and is oscillating  
C. The model has converged at a local minimum; the dataset does not contain sufficient variety to push loss below 1.2 with the current LoRA configuration  
D. paged_adamw_8bit introduces quantized momentum states that become inaccurate after ~200 steps

---

**Q3.** Your `postgres-sqlcoder-7b-v1` adapter produces exec correctness of 71% on your 100-example held-out test. A colleague argues this number is inflated because your held-out test was generated by the same synthetic pipeline as your training data. What is the strongest rebuttal?

A. The held-out test was never used in training; distribution similarity is expected and does not imply data leakage  
B. Synthetic test sets are always worse than human-annotated ones so the 71% is a lower bound  
C. The held-out test uses different random seeds for schema generation, so it is independent of training data  
D. Execution-based eval is immune to dataset bias because it measures runtime behavior, not text similarity

---

**Q4.** You want to publish your model to HuggingFace Hub. The base model `Qwen/Qwen2.5-Coder-7B` uses the Apache 2.0 license. Which of the following statements about licensing your adapter is correct?

A. You must use the same Apache 2.0 license because the adapter weights are a derivative of the base model  
B. You can use any license you choose because adapter weights are separate files and not legally derived from the base  
C. You cannot publish the adapter publicly because Qwen's license restricts redistribution of fine-tuned weights  
D. You should use Creative Commons CC-BY-4.0 because that is the standard for ML model artifacts

---

**Q5.** DoRA (introduced in Week 36) decomposes the LoRA weight update into magnitude and direction components. In your postgres-sqlcoder training, when would you prefer DoRA over standard LoRA?

A. When you have fewer than 5K training examples and want to maximize parameter efficiency  
B. When training loss is stable but eval exec correctness is improving slowly — suggesting the model is adapting direction but not magnitude appropriately  
C. When you need to reduce training time by 30% compared to standard LoRA  
D. When the base model was pre-trained with DoRA — using mismatched adapters causes instability

---

## Short Answer

**Q6.** Describe the complete inference pipeline for your `postgres-sqlcoder-7b-v1` model — from a user's natural language question and database schema to an executed SQL result. List every step in order and name the component responsible for each step.

---

**Q7.** Your QLoRA loss curve shows: rapid decrease from 2.1 to 1.4 in the first 100 steps, then a spike to 2.8 at step 150, then gradual recovery to 1.5 by step 400. Give 4 ranked hypotheses for what caused the spike at step 150, with the most likely first.

---

**Q8.** You are preparing a model card for your HuggingFace adapter. A colleague says: "Just write the training config — users only care about how it was trained." What critical information is missing from that approach, and why does it matter for a text-to-SQL model specifically?

---

## Scenario

**Q9.** A junior engineer on your team asks: "We fine-tuned a 7B model and got 71% execution correctness. GPT-4o gets 82% on the same test with zero-shot prompting. Was Phase 4 a waste of time? We should just use GPT-4o."

Write a response that: (a) explains why the comparison is not as damning as it looks, (b) names 2 concrete production advantages of a fine-tuned local model over GPT-4o for a text-to-SQL use case, and (c) describes the specific Phase 5 technique that is most likely to close — or flip — the 11-point gap.
